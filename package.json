{
  "name": "clear-connect-root",
  "version": "1.0.0",
  "description": "Root management scripts for full-stack build and deployment",
  "scripts": {
    "install-backend": "npm install --prefix backend",
    "install-frontend": "npm install --include=dev --prefix frontend-react",
    "install-all": "npm run install-backend && npm run install-frontend",
    "build-frontend": "npm run build --prefix frontend-react",
    "build": "npm run install-all && npm run build-frontend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
